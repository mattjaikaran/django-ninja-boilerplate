[project]
name = "django-ninja-matt"
version = "1.2.0"
description = "CLI tool for scaffolding Django Ninja Boilerplate projects"
authors = [{ name = "Matt Jaikaran", email = "info@mattjaikaran.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.13"
keywords = ["django", "ninja", "cli", "scaffolding", "boilerplate"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Framework :: Django",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Code Generators",
]

dependencies = [
    "typer>=0.12.0",
    "rich>=13.7.0",
    "jinja2>=3.1.0",
    "pyyaml>=6.0.0",
    "gitpython>=3.1.0",
    "questionary>=2.0.0",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.7.0",
]

[project.scripts]
django-ninja-matt = "django_ninja_matt.cli:app"
dnm = "django_ninja_matt.cli:app"

[project.urls]
homepage = "https://github.com/mattjaikaran/django-ninja-boilerplate"
repository = "https://github.com/mattjaikaran/django-ninja-boilerplate"
documentation = "https://github.com/mattjaikaran/django-ninja-boilerplate#readme"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/django_ninja_matt"]

[tool.ruff]
target-version = "py313"
line-length = 88
fix = true

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "C4", "SIM"]
ignore = ["E501", "B904", "UP042", "SIM102"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["-v", "--tb=short"]
