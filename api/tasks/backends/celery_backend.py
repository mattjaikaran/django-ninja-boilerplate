"""Celery backend configuration.

This is the default backend. Configuration lives in api/celery.py and
api/settings/common.py (CELERY_* settings).

No additional setup needed — Celery is always available as a core dependency.
"""
